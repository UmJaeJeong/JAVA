변수의 종류 		선언위치 	생성시기
------------------------------------------------------
클래스변수(static변수)	클래스영역
인스턴스변수	



메서드 = > 함수

1)선언부 => 반환값, 메서드명, 매개변수(인자값,파라메터, 아규먼츠)

2)구현부(정의부)


--------------------------------------------------------------------
static은 메모리상단에 올라간다.
그러므로 A.method호출 가능
하지만, instance메소드와 같은 경우는 new 연산자를 사용해야만 하므로
static메소드안에서는 instance메소드를 부를수 없다.
--------------------------------------------------------------------


call by value


call by reference



JVM의 메모리 구조 - 호출스택
- 메서드가 호출되면 수행에 필요한 메모리를 스택에 할당
- 


<재귀 호출>
-메서드 내에서 자기자신을 반복적으로 호출하는것.
-간결한 코드구현 가능
-반복문 보다 성능이 나쁨

ex)팩토리얼, 제곱, 트리운행, 폴더목록표시 등...
